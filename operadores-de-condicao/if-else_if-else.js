// if (condição) afirmação1

// Não retorna a mensagem.
const hora = 12;
if (hora < 12) {
  console.log('Bom dia!');
}

// Retorna Bom dia!
const hora = 11;
if (hora < 12) {
  console.log('Bom dia!');
}

// Retorna Bom dia!
const hora = 12;
if (hora <= 12) {
  console.log('Bom dia!');
}
